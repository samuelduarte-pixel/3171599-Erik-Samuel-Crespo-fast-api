[project]
name = "ejercicio-04-integracion"
version = "0.1.0"
description = "Ejercicio: Integración Pydantic + FastAPI"
requires-python = ">=3.14"
dependencies = [
    "fastapi>=0.128",
    "pydantic[email]>=2.10",
    "uvicorn>=0.40",
]

[tool.uv]
dev-dependencies = []
